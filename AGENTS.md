# rag

python rag client using chromedb.

src directory: src/rag_cli

# code style

* Use google code style (import modules, except allow importing typing types)
* Order methods in order caller ... called per method
* Only add documentation strings when the meaning is not clear from the implementation (they why not the what)
